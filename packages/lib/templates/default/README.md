# init scaffold (placeholder)

phase 3: the `init` command will copy this folder's contents to a new user project.
for now, see `packages/demo/` for the working example DS — it follows the same shape
the init command will produce.

import { defineConfig } from 'modo/config'

export default defineConfig({
  name: 'MUI',
  description:
    'Material UI v9 — components from @mui/material, default theme extracted into modo tokens.',
  css: './global.css',
})

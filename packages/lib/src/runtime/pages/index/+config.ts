import type { Config } from 'vike'
import vikeReact from 'vike-react/config'

export default {
  extends: vikeReact,
  title: 'modo-atomic-ui — phase 0',
  description: 'atomic design system renderer — phase 0 proof of concept',
} satisfies Config

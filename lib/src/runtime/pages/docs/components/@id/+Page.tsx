// /docs/components/:id — delegates to the shared ItemPage.
import { ItemPage } from '../../../../components/item-page'

export default function ComponentPage() {
  return <ItemPage tier="components" />
}
